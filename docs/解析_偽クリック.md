```js
window.mrWplace.wplaceMap.fire("click", {
  type: "click",
  target: window.mrWplace.wplaceMap,
  lngLat: { lng: 136.486758, lat: 21.211392 },
  point: { x: 458, y: 477 },
  // nullではなく、空の要素を持つオブジェクトを指定
  originalEvent: {
    target: window.mrWplace.wplaceMap.getCanvas(), // または document.body
  },
});
```
