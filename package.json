{
  "name": "mr-wplace",
  "version": "1.0.0",
  "license": "MPL-2.0",
  "scripts": {
    "build": "esbuild src/content.ts --bundle --outfile=dist/content.js --minify --target=chrome88",
    "dev": "npm run build -- --watch",
    "build:release": "bash ./scripts/build-release.sh"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.4",
    "esbuild": "^0.19.0"
  }
}